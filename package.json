{
  "name": "eleventy-l10n-demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run serve",
    "serve": "cross-env npm run clean && ELEVENTY_ENV=development npx eleventy --serve",
    "build": "cross-env npm run clean && ELEVENTY_ENV=production npx eleventy",
    "debug": "cross-env DEBUG=* npx eleventy",
    "clean": "rimraf dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.11.0",
    "cross-env": "^7.0.2",
    "prettier": "^2.0.5",
    "rimraf": "^3.0.2"
  }
}
